import PropTypes from 'prop-types';

const Footer = () => {
    return (
        <footer className="footer">
            footer
        </footer>
    )
}

Footer.propTypes = {}

export default Footer;
import PropTypes from 'prop-types';

const Header = () => {
    return (
        <header className="header">
            Header
        </header>
    )
}

Header.propTypes = {}

export default Header;